{
  "name": "jwu",
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "lib/**"
  ],
  "scripts": {
    "build": "npm install && node build.js && npx tsc && vite build",
    "clean": "rm -rf ./node_modules && rm -rf ./build && node clean.js"
  },
  "author": "Jonas De Decker",
  "devDependencies": {
    "@types/jasmine": "^5.1.9",
    "sass": "^1.92.1",
    "typescript": "^5.9.2",
    "vite": "^6.3.6",
    "vite-plugin-static-copy": "^3.1.3"
  },
  "directories": {
    "lib": "lib"
  }
}
